(function (window) {

    window.__env = window.__env || {};

    // API url
    window.__env.apiUrl = 'http://localhost:8877';

    // Base url
    window.__env.baseUrl = '/';


    // enable / disable debug mode
    window.__env.enableDebug = true;

    // cookie prefix
    COOKIE_PREFIX = 'MyApp_';


}(this));